<%= form_for(@comment) do |f| %>
	<div class="field">
	  <%= f.label :commenter %>
	</div>
	<div class="field">
	  <%= f.label :body %>
	</div>
	<div class="actions">
	  <%= f.submit %>
	</div>

<% end %>
